<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:behaviors="clr-namespace:BrewjournalApp.Behaviors"
             mc:Ignorable="d"
             x:Class="BrewjournalApp.Views.RecipesPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="My Recipes"
                   FontAttributes="Bold"
                   FontSize="Title"
                VerticalOptions="StartAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Entry Placeholder="Enter recipe search term..."
                   VerticalOptions="StartAndExpand"
                   HorizontalOptions="CenterAndExpand"
                   Text="{Binding SearchTerm}">
                <Entry.Behaviors>
                    <behaviors:EventToCommandBehavior EventName="TextChanged" Command="{Binding SearchRecipeCommand}"/>
                </Entry.Behaviors>
            </Entry>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>